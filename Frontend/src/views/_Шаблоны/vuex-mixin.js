import {
  mapActions,
  mapGetters,
  mapMutations,
} from 'vuex'

import {
} from '@/store/getter-types'

import {
} from '@/store/action-types'

import {
} from '@/store/mutation-types'

export const vuexMixin = {
  computed: {
    ...mapGetters([
    ]),

  },

  methods: {
    ...mapActions([
    ]),

    ...mapMutations([
    ]),
  },
}
